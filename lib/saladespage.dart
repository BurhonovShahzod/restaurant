import 'package:flutter/material.dart';

class SaladesPage extends StatelessWidget {
  const SaladesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Salades"),
      ),
    );
  }
}
